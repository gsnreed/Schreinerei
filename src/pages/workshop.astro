---
import Base from "../layouts/Base.astro";
import { SITE } from "../config/site";
---

<Base
  title={`${SITE.name} — ${SITE.workshop}`}
  description={`Informationen über die Werkstatt und Dienstleistungen für ${SITE.brand} ${SITE.name}.`}
  noindex
>
  <div></div>
</Base>

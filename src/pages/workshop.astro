---
import Base from "../layouts/Base.astro";
import { SITE } from "../config/site";
---

<Base
  title={`${SITE.name} — ${SITE.workshop}`}
  description={`Information about our workshop and services for ${SITE.name}.`}
  noindex
>
  <div></div>
</Base>

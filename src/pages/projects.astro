---
import Base from "../layouts/Base.astro";
import { SITE } from "../config/site";
---

<Base
  title={`${SITE.name} — ${SITE.projects}`}
  description={`Customer projects for ${SITE.name}.`}
  noindex
>
  <div></div>
</Base>
